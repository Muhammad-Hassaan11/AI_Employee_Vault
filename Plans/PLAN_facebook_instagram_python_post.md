# PLAN: Facebook/Instagram post — Python language

- Task source: direct prompt (no /Needs_Action file)
- Domain: business (social post per Business_Profile routing rules)
- Skill: social-post

## Steps

1. Read /Business/Business_Profile.md for voice and offer context — done
2. Draft a Facebook post about the Python language (plain text)
3. Draft an Instagram variant (with public image_url in frontmatter)
4. Save both to /Pending_Approval with `status: pending` (publishing is
   sensitive — never post directly)
5. Update Dashboard.md (Awaiting Approval) and log to /Logs + audit.jsonl
6. Move this plan to /Done
